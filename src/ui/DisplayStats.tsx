/** @jsx jsx */
import preact from 'preact/compat';
import { jsx } from '@emotion/react'

const DisplayStats = () => {
  return (
    <div
      css={{
        color: 'hotpink'
      }}
    >
      Hello
    </div>
  );
}

export default DisplayStats;
